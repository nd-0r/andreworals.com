import * as React from 'react'
import Layout from '../components/layout'

const Transcriptions = () => {
  return (
    <Layout pageTitle="Transcriptions">
      <p>some transcriptions</p>
    </Layout>
  )
}

export default Transcriptions